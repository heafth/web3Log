myproject/                 <- 模块名，全小写
├─ go.mod                  <- 模块配置文件
├─ main.go                 <- 可执行入口文件，文件名小写
├─ utils/                  <- 包名，全小写
│   ├─ string_utils.go     <- 文件名小写
│   └─ math_utils.go       <- 文件名小写
│
├─ models/                 <- 包名，全小写
│   ├─ person.go           <- 文件名小写
│   └─ order.go            <- 文件名小写
│
└─ handlers/               <- 包名，全小写
    ├─ user_handler.go     <- 文件名小写
    └─ product_handler.go  <- 文件名小写

首字母大小写规则：
包名、模块名、文件名：小写
函数/方法、结构体、常量、变量：
首字母大写 → 导出（外部可访问）
首字母小写 → 私有（仅包内可访问）
