Go 的相对路径确实和 Java（尤其是基于类路径 classpath 的加载方式）不太一样。区别在于：
Java：很多时候加载资源是基于 classpath 的，不依赖程序启动时的“当前目录”。无论你从哪里运行，只要资源在 classpath 中，getResource 等方法都能定位到。
Go：标准库的文件操作（os.ReadFile、os.Open 等）直接基于操作系统的绝对/相对路径。相对路径是相对于 运行时工作目录，也就是你执行程序时所在的目录。Go 本身并没有类似 classpath 的机制，更不会默认以源码文件所在位置为参照。
